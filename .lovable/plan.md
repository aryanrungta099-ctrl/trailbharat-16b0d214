Add `GOOGLE_AI_API_KEY_3` to the rotation in `/tmp/fill_treks.ts` alongside keys 1 & 2, then re-run against the remaining 108 missing treks. Script already skips dead keys (403/429) and stops when all are exhausted. Report final coverage.